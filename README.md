# PingUp – 1-on-1 casual video calling app

## Tech Stack:
- Frontend: React + Vite
- Backend: Node.js + WebRTC / Socket.io

## How to run:
1. cd frontend && npm install && npm run dev
2. cd backend && npm install && npm start
